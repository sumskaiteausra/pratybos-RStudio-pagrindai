# Nustatome lokalę
Sys.setlocale(locale = "Lithuanian")

# Užkrauname paketus, jei jų nėra - parsiunčiame ir tada užkrauname
if (!require(ggplot2)) {
	install.packages(ggplot2)
	library(ggplot2)
}

if (!require(magrittr)) {
	install.packages(magrittr)
	library(magrittr)
}


###! Rcmdr Options Begin !###
options(Rcmdr = list(console.output = FALSE))



